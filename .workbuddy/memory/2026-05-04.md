# 2026-05-04 工作记录

## 博客项目修复

### 问题：前台和后台同时打不开
- 根因：CNAME 文件在项目根目录，Vite 构建只打包 `public/` 目录
- 修复：将 CNAME 复制到 `public/CNAME`，Vite 构建时自动复制到 `dist/`
- 新增 `public/404.html` 解决 SPA 刷新 404 问题
- 在 `index.html` 添加路由恢复脚本
- 提交：`3cd0e10` fix: add CNAME to public dir and fix SPA routing for GitHub Pages

### 问题：TypeScript 构建失败
- `lucide-react` 没有导出 `GitHub` 图标（大小写问题）
- 修复：
  - 更新 `lucide-react` 到最新版（1.11.0 → 1.14.0）
  - 创建自定义 `GithubIcon` 组件（`src/components/GithubIcon.tsx`）
  - 修复 TypeScript 类型导入（`import type { Post }` 等）
  - 移除未使用变量（`generatePostsFileContent`, `owner`, `repo`, `getPassword`）
- 提交：`7caf2c5` fix: update lucide-react, add custom Github icon, fix TypeScript type imports

### 部署状态
- 代码已推送到 GitHub (`origin/main`)
- GitHub Actions 自动构建部署中
- 预计 2-3 分钟后可访问 https://shnenhuiketang.cloud
